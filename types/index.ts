export type RedSocial = {
  name: string,
  iconClass: string,
  url: string
}

export type PageContent = {
  title: string,
  content: string
  image?: string
}

export type Link = {
  title: string,
  url: string;
}